<h1>{{title}}</h1>
&nbsp;
<h2>All Projects</h2>
<a href="/projects/add" class="btn btn-primary">New Project</a>
<!-- Basic layout of the different Projects -->
<table class="table table-hover">
    <thead>
        <th>Project ID</th>
        <th>Project Name</th>
        <th>Project Type</th>
        <th>Start Date</th>
        <th>Estimate Finish Date</th>
        <th>Current Status</th>
        <th></th>
    </thead>
    <tbody>
        {{#each projectSet}}
        <tr>
            <td>{{this.projectId}}</a></td>
            <td>{{this.name}}</td>
            <td>{{this.projectType}}</td>
            <td>{{toShortDate this.startDate}}</td>
            <td>{{toShortDate this.estimateDate}}</td>
            <td>{{this.status}}</td>
            <td>
                <a href="/projects/edit/{{this._id}}" class="btn btn-info">Update</a>
                &nbsp;
                <a href="/projects/delete/{{this._id}}" class="btn btn-danger"
                    onclick="return comfirmDeletion()">Delete</a>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>